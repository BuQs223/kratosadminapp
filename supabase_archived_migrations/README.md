# Archived product migrations

These historical product/stock analytics migrations are deliberately outside the active Supabase migration path. The React Native admin client no longer exposes their screens or depends on their RPCs.

They are retained only for audit/reference purposes. Do not deploy, repair, or replay them without a separately approved product-data migration plan.
